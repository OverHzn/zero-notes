# Google OAuth Desktop App credentials.
# Copy to .env and fill in. See README "Set up Google OAuth" for how to get these.
# Alternatively, drop a JSON file at %APPDATA%/Zero Notes/oauth-config.json.

ZN_GOOGLE_CLIENT_ID=
ZN_GOOGLE_CLIENT_SECRET=
